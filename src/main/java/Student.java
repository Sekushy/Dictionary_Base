// Note: se pot utiliza și getters/setters dar aici folosim direct câmpurile publice.
public class Student {
    public String name;
    public int age;

    // Metodele de GET si SET pot fi si ele adaugate in momentul in care dorim sa ne facem campurile private.
}
